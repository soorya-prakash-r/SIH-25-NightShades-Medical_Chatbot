services:
  - type: web
    name: mitai-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: AI_API_KEY
        sync: false
      - key: VOICE_API_KEY
        sync: false
